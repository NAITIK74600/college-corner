import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // 'standalone' is only for self-hosted (shopkeeper ZIP). Vercel handles its own bundling.
  // output: 'standalone',
};

export default nextConfig;
